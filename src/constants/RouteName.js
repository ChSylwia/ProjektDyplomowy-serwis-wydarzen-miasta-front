const NavbarRouteName = {
  HOME: '/',
  WYDARZENIA: '/select',
  PROFIL: '/profile',
  LOGIN: '/login',
  REGISTER: '/register'
}

const RouteName = {
  ...NavbarRouteName
}

export { NavbarRouteName, RouteName }
