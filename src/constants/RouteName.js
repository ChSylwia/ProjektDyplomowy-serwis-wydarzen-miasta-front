const NavbarRouteName = {
  PROFILE: '/profile',
  HOME: '/',
  SELECT: '/select',
  LOGIN: '/login',
  REGISTER: '/register'
}

const RouteName = {
  ...NavbarRouteName
}

export { NavbarRouteName, RouteName }
